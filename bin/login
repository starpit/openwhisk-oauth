#!/usr/bin/env node

//
// the is for demo purposes: open a browser to initiate an oauth token
// acquisition
//
var providerName = process.argv[2] || "github";
require('../lib/client').loginViaBrowser(providerName);

